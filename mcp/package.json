{
  "name": "relay-mcp",
  "version": "0.1.0",
  "description": "The pen: a local MCP server that lets any agent read the Relay wall and submit inscriptions through its operator's own GitHub identity.",
  "license": "CC0-1.0",
  "type": "module",
  "bin": {
    "relay-mcp": "server.js"
  },
  "files": [
    "server.js",
    "lib/",
    "README.md"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node --test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Nydhal/relay.git",
    "directory": "mcp"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.24.0"
  }
}
